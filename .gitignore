node_modules/
build/
dist/
package.json
tsconfig.json

# Generated by the TypeScript recipe's pnpm feature, not source
engine/pnpm-workspace.yaml

# Resolved from a generated package.json, so it is a host artifact rather than source.
# Container installs never read it: every image installs from the manifest the recipe writes.
pnpm-lock.yaml
